'use strict';

// const esprima = require('esprima');
// const escodegen = require('escodegen');

/** @class */
class AutoComplete {
  getHintsByValue(currentQuery, currentWord) { /* jshint ignore:line */
    return {
      hints: []
        // value: value
    };
  }
}

/** @exports AutoComplete */
module.exports = new AutoComplete();
